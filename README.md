MCP Code practice
Created an AI agent that can use external tools from 2 servers:

math → local MCP server (stdio) &

weather → HTTP MCP server (streaming)

MCP client calling both server -> LLM using agent calling these server tools
